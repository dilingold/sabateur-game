package model;
import model.*;

/**
 * Created by johnny on 3/30/17.
 */
public class Validator {

    Boolean TRUE;
    public Validator() {
        //we also need a curse validator
    }

        public boolean checkMove(int cardType, int row, int column ){

        int  = Board.getInstance().getGameBoard(row,column);

        //takes in a location
        //takes in a card

        //checks if location is free
        //if occupied returns false

        //if unoccupied then check the surrounding cards and validate if its a valid move

        return TRUE;
    }
}
